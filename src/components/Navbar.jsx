import React from 'react'
import styles from '../styles'

function Navbar() {
    return (
        <main className={`py-4  bg-primary`}>
            <div className={`${styles.wrapper} flex justify-between text-white`}>

            </div>
        </main>
    )
}

export default Navbar